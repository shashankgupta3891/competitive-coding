#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

int main(){
    int x=-120;
    vector<int> num;

    // while(x!=0){

    //    num.push_back(x%10);
    //     x/=10;
    // }

    // int ans=0;
    // for(int i=num.size()-1; i>=0; i--){
    //   ans+=  pow(10, i)*num[num.size()-i-1];
    // }

    // cout<<ans;

    return 0;
}